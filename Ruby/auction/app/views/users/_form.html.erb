<%= form_for(user) do |f| %>

<%= render 'layouts/errors', model: user %>

	<div>
		<%= f.label :name %>
		<br>
		<%= f.text_field :name %>
	</div>

	<div>
		<%= f.label :email %>
		<br>
		<%= f.text_field :email %>
	</div>

	<div>
		<%= f.submit %>
	</div>

<% end %>